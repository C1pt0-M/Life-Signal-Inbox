# Life Signal Inbox 生活信号收件箱

`Life Signal Inbox`（生活信号收件箱）是一个 48 小时挑战项目。它把微信群通知、短信、课程公告、社区报名、截图文字这类碎片生活信息，整理成可执行的待办事项、日历事项和待确认项。

## 1. 项目痛点说明

生活里的关键信息经常藏在一大段通知里：截止时间、集合地点、需要携带的材料、联系人、费用、报名链接。用户看过不等于记住，也很难判断哪些信息还缺失、哪些时间会冲突。

这个项目不做普通聊天框，而是做一个生活消息处理台：输入通知后，系统先结构化，再验证，再进入待办清单。

## 2. 产品功能说明

- 待办清单页：左上角显示当前时间，右上角显示完成进度，下方展示提取后的待办事项。
- 四象限规划页：按重要/紧急程度分为红、黄、蓝、绿四个区域，支持选择某一天的规划，也支持拖动事项调整象限。
- `AI`（人工智能）助手页：支持文本交流和图片上传，上传图片后会先走 `OCR`（文字识别），再自动进入结构化提取和验证。
- 支持从通知文本提取标题、时间、地点、材料、联系人、原文依据和可信度。
- 支持可配置 `AI`（人工智能）结构化提取；未配置模型时自动使用本地规则兜底。
- 支持自动验证缺失字段、低可信度字段和时间冲突。
- 支持直接编辑待确认字段，并重新运行验证。
- 支持手动添加待办：事件、时间、重复规则、地点、备注和四象限分类都可以由用户自己填写。
- 支持保存到本地待办清单、导出 `ICS`（日历文件格式）、查看 `JSON`（结构化数据格式）。

## 3. Harness Engineering 工程化约束与反馈

本项目把 `Harness Engineering`（工程化约束与反馈）分成两层来说明：挑战要求更偏向“开发过程中如何用规则、工具和反馈循环约束 AI”，产品运行时也保留了同样的链路，方便演示。

### 3.1 开发过程中的 Harness

#### 上下文管理 / Agent Skill

开发时没有让 `AI`（人工智能）只根据一句话自由生成，而是先把规则写清楚：

- 提供 `AGENTS.md`（Agent 规则文档）指令，约定运行环境、沟通方式、敏感信息安全、提交格式和默认工作方式。
- 使用 `README.md`（项目说明文档）、`docs/harness-design.md`（Harness 设计说明）和 `docs/development-log.md`（开发过程记录）持续记录产品目标、边界和实现取舍。
- 把核心需求固定成明确流程：输入通知 -> 结构化提取 -> 自动验证 -> 待确认项编辑 -> 保存待办 -> 导出日历。
- 把后端拆成提取、验证、`OCR`（文字识别）、存储、`ICS`（日历文件格式）导出几个独立模块，减少 AI 修改代码时的上下文混乱。

#### 外部工具调用 / Tool 和 MCP

这里的 `MCP`（模型上下文协议）指开发环境里配置好的 MCP 服务器，不是项目代码里新搭一个服务器。本次开发把这些 MCP 服务器作为 Agent 的外部工具层使用，同时配合本地库和 `CLI`（命令行工具）突破模型限制：

- `server-memory`（知识图谱记忆服务）：用于保存实体、关系和观察，帮助保留重要上下文。
- `server-sequential-thinking`（顺序思考服务）：用于把复杂问题拆成步骤，减少直接跳到实现的风险。
- `server-time`（时间服务）：用于确认当前时间、时区和日期计算。
- `context7-mcp`（文档检索服务）：用于查询库文档和代码示例。
- `mcp-server-fetch`（网页抓取服务）：用于请求网页或接口，获取外部数据。

- `PowerShell`（Windows 命令行）：启动服务、检查进程、执行脚本。
- `git`（版本控制工具）：分阶段提交功能改动，避免一次性混在一起。
- `pytest`（Python 测试框架）：验证后端提取、验证、存储、导出逻辑。
- `npm test`（前端测试命令）和 `npm run build`（前端构建命令）：验证前端逻辑和打包结果。
- `react-doctor`（React 检查工具）：检查前端结构、可维护性和潜在问题。
- `Playwright`（浏览器自动化工具）：验证网页真实交互，例如四象限拖拽和待确认项编辑。
- `PaddleOCR`（本地文字识别库）：把真实截图转换成文本。
- `SQLite`（轻量数据库）：保存历史事项，避免演示数据只存在前端临时状态。

也就是说，这里没有把大模型当成唯一能力来源，而是让它配合本地工具、测试框架和命令行反馈完成开发。

#### 验证与反馈循环

开发时的 `Feedback Loop`（反馈循环）是：

```text
明确规则和数据结构
-> 编写或补充测试
-> 实现功能
-> 运行后端测试、前端测试、构建和浏览器验证
-> 根据失败信息继续修正
```

本项目已保留开发记录和验证记录，详见：

```text
docs/development-log.md
docs/feedback-loop-log.md
```

### 3.2 产品运行时的 Harness

#### 上下文管理

后端会把每次输入整理成结构化上下文，而不是直接让模型返回一段总结。

上下文至少包含：

- `raw_text`（原始文本）
- `source_type`（来源类型）
- `current_date`（当前日期）
- `timezone`（用户时区）
- `recognized_items`（已识别事项）
- `historical_items`（历史事项）

当前版本的 `AI`（人工智能）提取模块已经独立成 Harness 管道：先构造结构化上下文，再要求模型输出 `JSON`（结构化数据格式），再进入自动验证。未配置模型时，系统会使用本地规则兜底。

#### 外部工具调用

- 图片上传：通过后端 `PaddleOCR`（本地文字识别库）适配器返回截图文字，并接入同一套 `AI`（人工智能）结构化提取管道。
- 日历导出：后端把历史待办导出为 `ICS`（日历文件格式）。
- 本地保存：历史事项保存在本地 `SQLite`（轻量数据库）。
- 调试展示：前端可以直接查看完整 `JSON`（结构化数据格式）结果。

#### 验证与反馈循环

系统不会把 `AI`（人工智能）结果直接当最终答案使用，而是会自动检查：

- 每个事项是否有标题。
- 时间是否能解析成明确日期。
- 是否缺少地点、材料、联系人。
- 是否和历史事项存在时间冲突。
- 是否存在低可信度字段。

发现问题后，界面会显示风险提示和待确认项；用户确认后再加入待办清单。

如果模型输出不是合法 `JSON`（结构化数据格式），或者修正后仍不可用，系统会自动回退到本地规则提取，并在调试结果里记录兜底原因。

用户也可以在界面里补齐待确认字段，例如时间、地点、材料、联系人；补齐后调用后端统一验证接口重新检查。

## 4. 系统架构说明

项目采用前后端分离：

- 前端：`React`（前端框架） + `Vite`（前端构建工具），实现三页产品界面。
- 后端：`Python`（编程语言） + `FastAPI`（Python 后端框架），提供提取、验证、保存、导出接口。
- 数据：本地 `SQLite`（轻量数据库）保存历史事项。
- 可替换模块：提取模块、`OCR`（文字识别）模块、验证模块、日历导出模块都独立封装。

主要目录：

```text
backend/app/extractor.py      AI 提取适配层
backend/app/ai_extractor.py   AI Harness 提取管道
backend/app/validator.py      自动验证与反馈
backend/app/ocr.py            OCR 工具接口
backend/app/ics.py            ICS 日历导出
backend/app/storage.py        SQLite 本地历史保存
frontend/src/App.jsx          三页网页端界面
frontend/src/taskUtils.js     前端状态计算
```

运行后会生成本地数据库：

```text
backend/data/life_signal.db
```

这个文件只保存本机演示数据，不提交到仓库。

## 5. 如何运行项目

先启动后端：

```bash
cd backend
..\.venv\Scripts\python.exe -m pip install -r requirements.txt
..\.venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000
```

如果已经激活 `.venv`（虚拟环境），也可以直接使用：

```bash
python -m pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 8000
```

启用真实 `AI`（人工智能）提取，需要设置环境变量：

```bash
cd backend
copy .env.example .env
```

然后编辑 `backend/.env`（后端环境变量文件）：

```text
LIFE_SIGNAL_AI_PROVIDER=openai-compatible
LIFE_SIGNAL_AI_API_KEY=你的模型服务密钥
LIFE_SIGNAL_AI_MODEL=你的模型名称
LIFE_SIGNAL_AI_BASE_URL=https://api.openai.com/v1
```

`backend/.env`（后端环境变量文件）不会提交到仓库。也可以继续用系统环境变量配置，例如：

```bash
set LIFE_SIGNAL_AI_PROVIDER=openai-compatible
set LIFE_SIGNAL_AI_API_KEY=你的模型服务密钥
set LIFE_SIGNAL_AI_MODEL=你的模型名称
set LIFE_SIGNAL_AI_BASE_URL=https://api.openai.com/v1
```

不设置这些变量时，系统仍然能运行，会自动使用本地规则兜底。前端左侧会显示当前是 `AI Harness`（AI 工程化管道）还是规则兜底模式，但不会显示密钥。

也可以直接在前端左侧点击“前端配置模型”临时填写模型配置。这个配置会发送到后端运行内存中，本次后端进程有效；密钥不会写入 `.env`（环境变量文件），也不会从 `/api/config`（配置接口）返回给前端展示。

再启动前端：

```bash
cd frontend
npm install
npm run dev
```

打开：

```text
http://127.0.0.1:5173
```

运行测试：

```bash
cd backend
..\.venv\Scripts\python.exe -m pytest tests -q

cd frontend
npm test
npm run build
```

## 6. 演示样例

内置 3 条演示样例：

```text
家长群通知：本周三下午3点在学校礼堂开家长会，请携带学生手册，联系人王老师 13800138000。
```

```text
课程通知：4月21日17:30前在学习平台提交论文PDF，如有问题联系助教。
```

```text
社区公告：周末上午9点在社区中心集合参加志愿者服务，请带上水杯，联系人李老师 13900001111。
```

推荐录制路径：

1. 选择演示样例。
2. 点击“AI 提取”。
3. 查看待办字段、可信度、风险提示。
4. 如果有待确认字段，直接在“待确认项编辑”里补齐并重新验证。
5. 点击“加入待办清单”。
6. 切到“四象限规划”查看分类。
7. 返回待办页导出 `ICS`（日历文件格式）。

图片识别演示：

1. 在待办清单页点击“上传截图并提取”。
2. 选择一张包含中文通知的截图。
3. 系统会先用 `PaddleOCR`（本地文字识别库）提取文字。
4. 识别出的文字会自动进入 `AI`（人工智能）结构化提取和验证流程。
5. 页面会同时显示截图识别摘要、待办事项、风险提示和待确认项。

如果上传的是课表截图，系统会尽量按课程生成事项，例如“程序设计基础”。时间会优先从课表顶部日期行推导，地点会从 OCR 文本中提取；由于普通 OCR 容易丢失节次和行列关系，备注里会提示用户核对原图。

AI 助手页会把结果整理成固定格式：

```text
事件：
时间：
地点：
备注：
```

联系人默认不展示，备注中会显示课程/材料、可信度或原文依据。

## 7. 48 小时内完成范围说明

本版本优先保证核心演示闭环：

- 输入通知。
- 模拟 `AI`（人工智能）提取结构化事项。
- 自动验证质量。
- 展示待办、可信度、冲突和缺失信息。
- 保存到本地 `SQLite`（轻量数据库）待办清单。
- 导出 `ICS`（日历文件格式）。
- 支持图片上传，并使用本地 `PaddleOCR`（本地文字识别库）提取截图文字，再进入 `AI`（人工智能）/规则结构化提取。
- 支持用户手动添加待办和重复规则；“每个节假日”在当前 MVP 中按周六、周日导出。

暂不包含真实账号体系、真实短信/微信接入、真实大模型服务和多用户同步。

## 8. Harness 设计说明

更完整的 `Harness Engineering`（工程化约束与反馈）说明见：

```text
docs/harness-design.md
```
